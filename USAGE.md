# Ollama Benchmark Desktop 使用说明

## 一、环境准备

本机需要：

- Windows 10/11
- Node.js 20 或更高版本
- npm

被测服务器需要：

- 可以通过 SSH 登录
- 已运行下列任一推理服务：
  - **Ollama**（原生安装或 Docker 容器，含 `ollama-rocm` / `ollama-cuda` 等加速镜像）
  - **llama.cpp** 的 `llama-server`，以及 llama-swap 之类的前置代理
  - 任意 **OpenAI 兼容**服务（vLLM / LM Studio / 远程网关均可）

不再需要区分「装的是哪个加速版本的 Ollama」。加速与否由应用**实测**得出（见下），
安装 `ollama-rocm`、`ollama-cuda` 还是普通 `ollama` 由服务器自己决定。

候选端口如下，应用会逐个探测并把全部都列出来：

| 协议族 | 默认候选端口 |
| --- | --- |
| Ollama | `11434`、`11435`、`11437`、`11438` |
| llama.cpp / OpenAI 兼容 | `8082`、`8080`、`8000`、`10000`、`8081`、`1234` |

llama.cpp 的实际端口不是猜的——应用会从进程命令行与系统监听表反推，所以非标准端口也能发现。
仍可在连接页手填「推理 API 端口」覆盖。

应用通过 SSH 隧道访问远端 API，不要求把 11434 暴露到公网。

## 二、启动应用

在项目根目录执行：

```powershell
npm install
npm run dev
```

开发模式会启动 Electron 桌面窗口。

验证代码：

```powershell
npm test
npm run typecheck
npm run build
```

生成 Windows 未安装目录：

```powershell
npm run package
```

打包结果默认位于 `dist/` 或 electron-builder 输出目录中。

## 三、首次测试流程

### 1. 连接服务器

填写：

- 服务器 IP 或主机名
- SSH 端口，默认 `22`
- SSH 用户名
- SSH 密码
- 推理 API 端口（可选，留空则自动枚举）
- API Host / API Key（仅“OpenAI 兼容·远程”需要）

**推理后端**只问一个协议问题，三选一：

| 选项 | 对应服务 | 说明 |
| --- | --- | --- |
| `Ollama` | 原生 Ollama、`ollama-*` 加速镜像、Ollama Docker | 走 Ollama 原生 API（`/api/tags`、`/api/ps`） |
| `llama.cpp · OpenAI 兼容` | `llama-server`、llama-swap | 走 `/v1/models`、`/v1/chat/completions` |
| `OpenAI 兼容 · 远程 + Key` | vLLM、LM Studio、远程网关 | 与上一项同协议，额外带 API Host / Key |

选择后应用只探测对应协议族的服务，**不会安装、启动、停止或修改服务器上的任何服务**。

点击“连接并检查环境”后，应用会：

1. 用 **2 条批量命令**采集系统信息（不逐条 exec，避免 SSH 通道开销）：
   监听端口表、进程列表、二进制路径、GPU、内存/存储、操作系统。
2. 逐个探测上表里的**全部候选端口**，以及从进程/监听表反推出来的端口。
3. 汇总为一个端点列表返回，**不会首个命中就停**。若你填了端口，该端口优先。

应用会自动检查并展示：

- 所有可达端点的端口、模型数量、加速实测状态
- 原生 Ollama / Docker Ollama / llama-server
- AMD/NVIDIA GPU 型号（含 iGPU）
- GPU/CPU、内存、存储与操作系统信息
- Ollama/llama.cpp 版本与已安装模型

> ⚠️ 探测会逐个枚举候选端点，服务器较慢时约需 **30–60 秒**，界面会显示“正在枚举端点…”。

#### 加速实测（不是选项）

环境页的「加速实测」是**测量结果**，不是让你选的配置项：

- 优先读 Ollama `/api/ps` 的 `size_vram / size` 比值——接近 1 说明权重基本全在显存里；
- 读不到则解析 `llama-server` 启动参数 `--n-gpu-layers` / `-ngl`；
- 判定为 **GPU** / **部分卸载** / **CPU** / **未知** 四档，报告里以“加速实测”一行呈现。

这样报告的结论无法被人为选错——你无法让一个纯 CPU 推理“看起来”跑在 ROCm 上。

#### 候选端点与手动切换

环境页把命中的端点标为「命中端点」，其余列为「候选端点」表格，每行显示
端口、模型数、加速状态；失败的行会标出失败原因。任何时候都可以点某一行
**手动切换**到该端点，切换后会重新读取模型列表与版本。
### 2. 配置测试

选择：

- 一个或多个模型
- 输入 token 长度
- 并发数
- 测试轮次
- 最大输出 token 数

每个组合都会独立测试。例如：

```text
1 个模型 × 4 个输入长度 × 2 个并发数 × 2 轮
```

请求数为：

```text
1 × 4 × (1 + 2) × 2 = 24 次
```

建议第一次使用小规模冒烟测试：

```text
模型：选择 1 个
输入长度：100、1K、4K、8K
并发数：1、2
轮次：2
最大输出：50
```

完整默认矩阵可能产生约 6,200 次请求，尤其是 122B 模型和 64K 输入，执行时间可能很长。

### 3. 执行测试

点击“开始测试”后，应用会：

1. 对每个模型执行 warm-up，请求不计入统计。
2. 每轮同时启动指定数量的请求。
3. 使用 `stream=true` 接收响应。
4. 记录第一个非空流分块作为 TTFT。
5. 使用 Ollama 最终分块中的 `eval_count / eval_duration` 计算 Decode。

可以在执行页查看当前模型、输入长度、并发数、轮次和总进度，也可以取消测试。

### 4. 查看和导出结果

结果页展示：

- 平均 TTFT
- P95 TTFT
- Decode 速度
- 样本数
- 失败率

点击“导出三种报告”，选择输出目录后会生成：

- Markdown：适合 Git、邮件和归档
- HTML：适合浏览器查看
- Word：适合正式交付

## 四、指标解释

```text
TTFT = 客户端请求开始到首个非空流式响应分块到达的时间
Decode(tok/s) = eval_count / (eval_duration / 1,000,000,000)
```

报告同时记录服务端 `prompt_eval_duration`，用于区分模型 Prefill 和 SSH 网络/排队耗时。

输入长度以 Ollama 返回的 `prompt_eval_count` 为准；目标长度是配置值，实际值可能因模型模板和 tokenizer 不同而略有差异。

## 五、常见问题

### 找不到 Ollama

确认服务器上可以执行：

```bash
curl http://127.0.0.1:11434/api/version
```

Docker 部署需要确保容器端口已映射，或者 SSH 用户可以访问 Docker bridge 网络。

### 只找到一个端点，但服务器上明显跑了多个

先看「候选端点」表格里失败行的原因——每个端点的失败原因都会带端口号记在警告里
（连接被拒 / 超时 / HTTP 状态码 / 返回的不是 JSON 等），据此判断是端口没起、
被防火墙拦了，还是该端口被别的服务占用（这时会返回 HTML，应用会提示
“该端口似乎返回了网页而非 API”）。

如果服务监听在非标准端口，直接在看板上填「推理 API 端口」，该端口会被优先探测。

### 加速实测显示「CPU」，但服务器有显卡

三种可能，按顺序排查：

1. 模型太大装不进显存，Ollama 会回落到 CPU——换小模型或用更低的量化级别。
2. 服务端确实没启用 GPU 层，检查服务器上 Ollama/llama-server 的启动参数与日志。
3. `/api/ps` 里没有正在加载的模型（`size` 为 0），此时比值无意义，会判为“未知”。
   先发一个请求把模型载入内存再看。

应用不会自动替换或重启服务器上的服务，只做只读探测。

### 长输入失败或显存不足

降低输入长度、减少并发数，或确认模型上下文窗口和服务器显存足够。64K 输入不适合直接对所有模型执行完整矩阵。

### TTFT 与服务端 Prefill 不一致

客户端 TTFT 包含 SSH 隧道、网络往返和服务端排队；服务端 Prefill 是 Ollama 返回的内部阶段耗时，两者不应完全相等。

## 六、安全注意事项

- 不要把 SSH 端口或 Ollama 11434 暴露到公网。
- 密码不会写入测试结果或报告。
- 不要把生成的报告上传到包含服务器敏感信息的公共位置。
- 生产环境建议改用 SSH 私钥认证。

## 七、模型能力横评（V1.3）

连接服务器并完成环境检查后，顶部可切换到“模型能力横评”。该功能与性能测试独立，不会修改原有性能测试配置或指标。

- 评估维度：写代码、翻译、文言文、数学、逻辑、中文常识、创意写作；支持多选维度。
- 内置 7 个维度、14 道独立题，每题 5 分；单模型满分会随选择的维度和自定义题目变化。
- 评分在本机按固定规则完成：代码检查关键结构，翻译/文言文检查关键词组，数学检查数值容差，逻辑和常识匹配标准答案。
- 支持多模型横向比较，页面显示进度、逐题日志、总分、各维得分、Decode(tok/s) 和柱状图/雷达图。
- 能力报告支持 Word、PDF、Markdown、HTML；Markdown 报告会同时生成柱状图和雷达图 SVG 文件。

建议先选择 1—2 个小模型进行冒烟评测，确认 API 可用后再扩大模型范围。取消或中断后，页面会保留已完成题目并可导出部分报告；再次开始会重新执行完整题库，未完成模型会在报告中标记完成度。

### 能力评测的限制

能力评分是可复现的规则评分，不等同于人工专家评审；代码题不执行模型生成代码，避免对服务器或本机产生副作用。模型输出包含思考内容时，思考文本不会计入答案评分。

### 自定义题库

在“模型能力横评”页面的“题库管理”区域可以：

- 点击“新增题目”，按 JSON 模板新增一题或多题；
- 点击“下载 Excel 格式模板”获取包含“题库”和“规则说明”工作表的模板；
- 点击“导入 Excel 题库”导入 `.xlsx`/`.xls`；旧 JSON 文件仍兼容；
- 点击“导出 Excel 题库”导出内置题目和自定义题目的完整题库。
题目支持四种规则：`keywords`（关键词组）、`number`（数值答案）、`exact`（别名匹配）和 `code`（代码结构检查）。题目 ID 在同一题库中必须唯一，单题分值范围为 0—10 分。

## 八、高级场景评测（V2.0）

顶部切换到“高级场景评测”即可运行 `llm-bench` 测试方案（`llm-bench-2026.08`）的原生移植版本，无需 Python 运行时。该入口与性能测试、能力横评相互独立。

### 1. 场景选择

勾选需要执行的场景，默认启用 Latency 基准、并发阶梯压测、公平性分析：

- **Latency 基准**：固定并发 1，扫描 input×output 档位，摸底单请求性能基线（TTFT / E2E / P95 / TPOT / ITL / Decode / Prefill / Overall TPS）。
- **并发阶梯压测**：阶梯 1→2→4→… 探首个失败档，再二分收敛。判定门槛为 **成功率 ≥ 99% 且 P95-E2E ≤ 基线(并发=1) × 1.5**；推荐下游并发为确认最大并发的 50%，并对确认档做稳态复测。
- **最大上下文探测**：输入长度递增，首个上下文超限即停止。
- **稳态漂移**：固定并发长时压测，按分钟分桶监测 P95 漂移。
- **公平性分析**：复用并发压测样本，分析请求间延迟离散度与饿死请求（CV / max(median) / P99(P50) / 饿死数）。
- **长文本召回**：长文本中埋入关键事实（needle），按长度档统计召回率。

### 2. 关键参数

- 并发自适应压测：开启后自动阶梯 + 二分；可设并发上限（默认 256）。
- 稳态时长 / 稳态并发、长文本重复次数、请求超时、确定性种子（默认 42，保证可复现的填充 prompt）。

### 3. 执行与导出

页面显示当前场景、进度条、实时日志；完成后展示各场景结论卡片（并发通过/失败档、上下文上限、漂移判定、公平性、召回率等）并支持导出 Markdown / HTML / Word / PDF 报告。每轮还会生成**退化检测**结论（空响应率、重复率、finish_reason 分布、上下文超限等告警）。

### 4. 质量基准桥接

“质量基准”套餐（ceval / cmmlu / truthful_qa / simple_qa / mmlu_pro / gpqa / gsm8k / humaneval / ifeval）已作为配置内置，工具会给出一条可直接运行的 `evalscope` 桥接命令；数据集类基准依赖外部模型数据集，不在桌面端内执行。

### 5. 注意事项

- 并发压测会对服务器施加真实负载，请在生产环境谨慎使用；建议先用小模型冒烟。
- 填充 prompt 使用确定性 PRNG（mulberry32）与种子步长 7，与 Python CLI 同一 (seed, count) 可复现同一 prompt 集；token 数为 4 字符/token 的估算值，实际输入 token 以服务端返回为准。
